name: ras1234
services:
  frontend:
    # image: ghcr.io/gold007-dev/ras-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - 12345:3000
    extra_hosts:
      - "host.docker.internal:host-gateway"
